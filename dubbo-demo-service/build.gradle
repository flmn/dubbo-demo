springBoot {
    buildInfo()
}

bootJar {
    launchScript()
}

dependencies {
    implementation(
            libraries.sb_core,
            libraries.sb_jpa,
            libraries.sb_dubbo,

            project(':dubbo-demo-api'),
    )

    runtimeOnly(
            libraries.mysql,
    )

    testImplementation(
            libraries.sb_test,
    )
}
